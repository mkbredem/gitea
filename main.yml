- name: Install gitea 
  hosts: "{{ target_hosts }}"
  roles: 
    - d01jlr.gitea
  vars: 
    gitea_fqdn: 'git.example.com'
    gitea_root_url: 'https://git.example.com'
    gitea_protocol: http
    gitea_start_ssh: true